export interface ImageResult {
    id: string;
    urls: {
      thumb: string;
    };
    alt_description: string;
  }

  export interface SerpApiResult {
    title: string;
    link: string;
    id: string;
    thumbnail: string;
  }
